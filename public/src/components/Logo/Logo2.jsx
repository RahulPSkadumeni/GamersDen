import * as React from "react";

import "./Logo2.css";
const Logo2 = (props) => {
  return (
    <div className={`action-add-friend ${props.className || ""}`}>
     
      <span className="qtzmvqkmugjpy">{props.sybddkpxuwtmn || "􀜕"}</span>
    </div>
  );
};
export default Logo2;